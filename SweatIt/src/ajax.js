/*const apiHost=''


export default {
  async fetchInitialDeals() {
    try {
    let response = await fetch(apiHost + '/api/');
    let responseJson = await response.json();
    return responseJson;
  } catch (error) {
    console.error(error);
  }
  }
};
*/
